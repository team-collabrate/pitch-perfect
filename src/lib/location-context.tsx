"use client";
// location-context removed — use static coords or a new provider if needed
// This file intentionally left minimal to avoid import errors while the
// project transitions away from the LocationProvider approach.

export const REMOVED_LOCATION_CONTEXT = true;
